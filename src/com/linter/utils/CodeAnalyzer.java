package com.linter.utils;
import java.util.*;

public class CodeAnalyzer {
    public static List<String> findErrors(List<String> fileLines) {
        List<String> errors = new ArrayList<>();
        
        for (int i = 0; i < fileLines.size(); i++) {
        	
        }
        
        return errors;
    }
}
